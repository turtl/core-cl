NW_TARGET=@nw_target@

ifeq ($(OS),Windows_NT)
	LIB=turtl.lib
else
	LIB=turtl.so
endif

all:

	node-gyp clean configure
	cp ../../build/$(LIB) ./build
	node-gyp build

nw:

	nw-gyp clean configure --target=$(NW_TARGET)
	cp ../../build/$(LIB) ./build
	nw-gyp build

clean:

	rm -rf build/

