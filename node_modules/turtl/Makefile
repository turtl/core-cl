ifeq ($(OS),Windows_NT)
	LIB=turtl.lib
else
	LIB=turtl.so
endif

all:
	node-gyp clean configure
	cp ../../build/$(LIB) ./build
	node-gyp build

nw:
	nw-gyp clean configure --target=0.9.2
	cp ../../build/$(LIB) ./build
	nw-gyp build

